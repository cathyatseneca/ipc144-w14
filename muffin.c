/*this program will calculate the total cost of buying muffins which are
  sold at 80 cents each.  Program will ask the user to enter in the 
  number of muffins they wish to buy.  It will then calculate the 
  cost of the muffins and if necessary the tax (13% hst).  tax is only applied
  if there are less than 6 muffins purchased.
*/

#include <stdio.h>
int main(void){
    int numMuffins;
    //ask user for number of muffins
    //read that number in

    if( numMuffins < 6){
	//calculate price with hst
    }
    else{
       //calculate price without hst
    }
    //print out the calculated price
}

